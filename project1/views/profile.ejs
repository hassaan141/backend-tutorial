<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="w-full h-full bg-zinc-900">
  <div class="w-full h-full bg-zinc-900 p-10 text-white">
    <form action="/post" method="post">
      <div class="flex justify-between items-center mb-5">
        <h2 class="text-3xl"><span class="font-light">Hello,</span> <%= user.name%></h2>
        <a href="/logout" class="px-5 py-3 bg-red-500 border-1 rounded-lg"> Logout</a>
      </div>
      <h5 class="mb-5">Create a post</h5>
      <form action="">
        <textarea placeholder="What's on your mind?" class="p-5 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md block"  name="content"></textarea>
        <input value="Create New Post" type="submit" class="text-sm py-2 w-40 bg-blue-500 block rounded-md mt-2 " >
      </form>
      <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Posts.</h3>
        <div class="postcontainer mt-5 ">
          <% user.posts.reverse().forEach((post)=>{ %>
            <div class="post w-1/3 mb-4 p-4 rounded-md border-2 border-zinc-800 bg-border-700">
              <h4 class="text-blue-500 mb-2">@<%= user.username%></h4>
              <p class="text-sm tracking-tight"><%= post.content%></p>
              <div class="btns flex my-2 gap-3">
                <a class="text-blue-500" href="">Like</a>
                <a class="text-zinc-600" href="">Edit</a>
              </div>
            </div>
            <% })%>
        </div>
      </div>
      

  
    </form>

  </div>
</body>
</html>

