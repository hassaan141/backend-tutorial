<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 ">
      <div class="form p-10 text-white">
       <form action="/create" method="post">  <!-- the action sends a post to our backend -->
          <input class="block w-full rounded-md outline-none px-5 py-2 bg-zinc-800" type="text" name="title" placeholder="Title Goes Here">
          <textarea class="block w-full rounded-md outline-none mt-2 px-5 py-2 resize-none bg-zinc-800" name="body" placeholder="Write your task detail here" ></textarea> 
          <input class="mt-2 bg-blue-600 rounded-md px-2 py-2" type="submit" value="Create Task">
        </form>
      </div>
      <div class="tasks p-10 flex gap-3 flex-wrap">
        <% if (files.length >0) { %>
          <% files.forEach(function(val){ %>
            <div class="task w-72 px-3 py-4 bg-zinc-800 text-white ">
              <h1 class="text-white text-2xl tracking-tight"><%= val%></h1>
              <div class="flex w-full justify-between items-center  mt-3">
                  <a class="text-blue-600 inline-block" href="/file/<%= val %>">Read More</a>
                  <a class="text-zinc-600" href="/edit/<%= val %>">edit</a>
              </div>
            </div>
          <% }) %>
        <% } else { %>
          <h3 class="text-zinc-600">No Tasks Yet</h3>
          <%  } %>
        
      </div>
    </div>
</body>
</html>